name: Build & Push to GHCR

on:
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - 'handler.py'
      - 'entrypoint.sh'
      - '.github/workflows/build.yml'
  workflow_dispatch:
    inputs:
      prebake_model:
        description: 'HF repo to pre-bake into the image (empty = skip)'
        required: false
        default: 'AEON-7/Qwen3.6-27B-AEON-Ultimate-Uncensored-NVFP4'

jobs:
  build:
    runs-on: blacksmith-16vcpu-ubuntu-2404
    timeout-minutes: 45
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker builder (Blacksmith)
        uses: useblacksmith/setup-docker-builder@v1

      - name: Lowercase owner
        id: owner
        run: echo "lc=$(echo ${{ github.repository_owner }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Build & Push
        uses: useblacksmith/build-push-action@v2
        with:
          context: .
          platforms: linux/amd64
          push: true
          build-args: |
            PREBAKE_MODEL=${{ github.event.inputs.prebake_model || 'AEON-7/Qwen3.6-27B-AEON-Ultimate-Uncensored-NVFP4' }}
            HF_TOKEN=${{ secrets.HF_TOKEN }}
          tags: |
            ghcr.io/${{ steps.owner.outputs.lc }}/vllm-worker:latest
            ghcr.io/${{ steps.owner.outputs.lc }}/vllm-worker:${{ github.sha }}
